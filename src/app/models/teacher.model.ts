export class Teacher {
    id: number;
    serial: string;
    firstName: string;
    name: string;

    constructor(id: number, serial: string, firstName: string, name: string) {
        this.id = id;
        this.serial = serial;
        this.firstName = firstName;
        this.name = name;
    }
}
